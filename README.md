This repo demonstrates a bug with github action's composite actions feature.

On the main branch it has just one github actions workflow, called 'working-action'.  It is called by dispatch and just prints a string.

Later, a composite action will be created on a different branch.
